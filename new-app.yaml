apiVersion: apps/v1
kind: Deployment
metadata:
  name: new-app
spec:
  replicas: 8
  selector:
    matchLabels:
      app: canary
  template:
    metadata:
      labels:
        app: canary
    spec:
      containers:
      - name: test
        image: quay.io/shruti1988/production:v1
